-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.Daily_Expenses (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  Date date NOT NULL,
  material_cost double precision,
  material_type text,
  miscellaneous_Cost double precision,
  miscellaneous_item text,
  chai_pani_cost double precision,
  Total_Pay double precision,
  CONSTRAINT Daily_Expenses_pkey PRIMARY KEY (id)
);
CREATE TABLE public.Worker_Expense (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  date date NOT NULL,
  name text NOT NULL,
  Amt_Paid numeric,
  worker_id integer,
  CONSTRAINT Worker_Expense_pkey PRIMARY KEY (id)
);
CREATE TABLE public.billno (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  billno bigint,
  CONSTRAINT billno_pkey PRIMARY KEY (id)
);
CREATE TABLE public.bills (
  customer_name text NOT NULL,
  mobile_number text NOT NULL,
  date_issue date NOT NULL,
  delivery_date date NOT NULL,
  suit_qty integer,
  safari_qty integer,
  pant_qty integer,
  shirt_qty integer,
  sadri_qty integer,
  total_qty integer,
  today_date date NOT NULL,
  due_date date NOT NULL,
  total_amt double precision NOT NULL,
  payment_mode text NOT NULL,
  payment_status text NOT NULL,
  payment_amount double precision,
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  CONSTRAINT bills_pkey PRIMARY KEY (id)
);
CREATE TABLE public.measurements (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  phone_number text UNIQUE,
  pant_length double precision,
  pant_kamar double precision,
  pant_hips double precision,
  pant_waist double precision,
  pant_ghutna double precision,
  pant_bottom double precision,
  pant_seat double precision,
  SideP_Cross text,
  Plates text,
  Belt text,
  Back_P text,
  WP text,
  shirt_length text,
  shirt_body text,
  shirt_loose text,
  shirt_shoulder double precision,
  shirt_astin double precision,
  shirt_collar double precision,
  shirt_aloose double precision,
  Callar text,
  Cuff text,
  Pkt text,
  LooseShirt text,
  DT_TT text,
  extra_measurements text,
  created_at timestamp without time zone,
  updated_at timestamp without time zone,
  CONSTRAINT measurements_pkey PRIMARY KEY (id)
);
CREATE TABLE public.order_worker_association (
  id integer NOT NULL DEFAULT nextval('order_worker_association_id_seq'::regclass),
  order_id integer NOT NULL,
  worker_id integer NOT NULL,
  CONSTRAINT order_worker_association_pkey PRIMARY KEY (order_id, worker_id),
  CONSTRAINT order_worker_association_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT order_worker_association_worker_id_fkey FOREIGN KEY (worker_id) REFERENCES public.workers(id)
);
CREATE TABLE public.orders (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  garment_type text NOT NULL,
  status text NOT NULL,
  order_date date NOT NULL,
  due_date date NOT NULL,
  total_amt double precision NOT NULL,
  payment_mode text NOT NULL,
  payment_status text NOT NULL,
  payment_amount double precision NOT NULL,
  Work_pay double precision,
  billnumberinput2 double precision,
  bill_id integer NOT NULL,
  updated_at date,
  CONSTRAINT orders_pkey PRIMARY KEY (id),
  CONSTRAINT orders_bill_id_fkey FOREIGN KEY (bill_id) REFERENCES public.bills(id)
);
CREATE TABLE public.workers (
  id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  name text,
  number text,
  Rate double precision,
  Suit double precision,
  Jacket double precision,
  Sadri double precision,
  Others double precision,
  CONSTRAINT workers_pkey PRIMARY KEY (id)
);